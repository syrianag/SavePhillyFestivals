# UI issue 
The admin will needs controls (CRUD Operations) to modify data in the databse so we should include those. The UI in the Admin portal also doesn't include date filters. The homepage has to really large "Featured" festivials that are not fitting into the view port. the entire site should be responsive web design adjust to all screens nicely. 

# Featured events
There's no clear control on how the client would mark a feastival as featured, remove a featured event or update a featured events 

# Sponor Links/ads 
Additionally, how does the sponsor ads work or show up? I'm not clear on how to deliver this product as fully featured based on the call and @feature-list-client-call-2026-08-06.md. I imaged this to work similar to the roadmap_clone/apps/launchstack project allowing the user to place sponosered added on the left, right and footer of the app. Rather these are Google Ads Sense ads or custom ads the feature should allow the client to configre and decide. Let's dicuss how this currently works and how we can improve it. 

# Calendar Issue 
There's also something wrong with the Calendar clicks for locating an event when clicking on a date. I was unable to find several events. the save calender form on the Calendar page doesn't seem to match the site theme `for="saved-schedule-heading-desktop-email"` the [admin/festival/](http://localhost:3000/admin/festivals/5a6f8cb9-27cd-4389-83dd-66c3f8a2cd9a) has a simlar issue for all events

# Events Issue 
Also we should filter the view to show upcoming events from the current month forward. Let's move all 2025 events to unpublished as well 

# Setting page 
the UI can be enhanced as there is a lot of negative or white space that is unused and/or not used in a modern way. 